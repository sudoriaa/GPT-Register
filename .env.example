# Copy to .env and fill values. Never commit .env
#   cp .env.example .env

# 所有 WebUI 可编辑配置都使用 .env 覆盖 config/*.py 默认值。

# ---- WebUI 授权 ----
# WebUI 登录授权码；仅保存在 .env，避免出现在进程命令行中
WEBUI_AUTH_CODE=
# 可选：Session Cookie 签名密钥；留空时从固定授权码派生，修改授权码会使已有登录失效
WEBUI_SESSION_SECRET=


# ---- 功能开关 ----
# 启用 Codex OAuth: 注册成功后自动跑 Codex 授权（全新session+接码），落盘 codex-邮箱.json
ENABLE_CODEX_AUTO=

# ---- 注册方式 ----
# 注册驱动: protocol=纯协议；roxy=RoxyBrowser；cloak=CloakBrowser；browser_use=Browser Use Cloud+Playwright
REGISTRATION_DRIVER=
# 批量任务启动间隔（秒）；默认每 2 秒启动一个窗口
BATCH_STAGGER=2

# ---- CloakBrowser ----
# Cloak无头: True=无头运行；False=显示浏览器窗口
CLOAK_HEADLESS=
# Cloak人工行为: 启用 CloakBrowser humanize 鼠标/键盘/滚动行为
CLOAK_HUMANIZE=
# Cloak按出口定位: 按当前出口 IP 自动匹配时区/语言/WebRTC IP；支持显式代理、系统代理/VPN
CLOAK_GEOIP=
# Cloak语言: 留空自动；日本可填 ja-JP，美国 en-US
CLOAK_LOCALE=
# Cloak时区: 留空自动；日本可填 Asia/Tokyo，美国 America/Los_Angeles
CLOAK_TIMEZONE=
# Cloak使用代理: 把本项目传入或代理池抽取的代理传给 CloakBrowser
CLOAK_USE_PROXY=
# Cloak License: free/Pro key；留空使用旧版无密钥 binary
CLOAK_LICENSE_KEY=
# Cloak指纹Seed: 留空每次随机；固定值可保持同一指纹
CLOAK_FINGERPRINT_SEED=
# Cloak用户目录: 留空使用临时上下文；填写路径则持久化 cookies/cache
CLOAK_USER_DATA_DIR=
# Cloak超时: 页面和元素等待超时时间，秒
CLOAK_SELENIUM_TIMEOUT=
# 保留Cloak浏览器: 调试时开启，任务结束后不自动关闭
CLOAK_KEEP_BROWSER_OPEN=

# ---- Browser Use ----
# Browser Use API Key: 保存在 .env（BROWSER_USE_API_KEY），不写回 config/*.py
BROWSER_USE_API_KEY=
# 代理国家代码: 两位国家码，如 jp/us/sg；配合 Browser Use 内置 residential proxy
BROWSER_USE_PROXY_COUNTRY_CODE=
# 使用内置代理: True=连接参数带 proxyCountryCode；False=不强制传国家代理参数
BROWSER_USE_USE_PROXY=
# Profile ID: 可选。填写则复用 Browser Use profile 的 cookies/localStorage；批量建议留空
BROWSER_USE_PROFILE_ID=
# CDP 地址: 默认 wss://connect.browser-use.com
BROWSER_USE_CDP_BASE=
# 操作超时(秒): Playwright 默认操作超时
BROWSER_USE_TIMEOUT=
# 快速模式: 减少 Browser Use 额外等待和 humanize 延迟；建议开启，异常排查时可关闭
BROWSER_USE_FAST_MODE=
# 耗时日志: 打印 Browser Use 各阶段耗时：连接、打开页面、邮箱、OTP、手机、callback
BROWSER_USE_LOG_TIMING=
# 保留远端会话: 调试时可不主动 browser.close()；默认 False
BROWSER_USE_KEEP_BROWSER_OPEN=
# 起始 URL: 默认 https://chatgpt.com/auth/login
BROWSER_USE_START_URL=

# ---- Skyvern ----
# Skyvern API Key：用于创建 Browser Session
SKYVERN_API_KEY=
# API 地址：默认 https://api.skyvern.com
SKYVERN_API_BASE=
# Browser Session 超时（分钟）
SKYVERN_BROWSER_SESSION_TIMEOUT=
# 可选：复用 Skyvern browser profile
SKYVERN_BROWSER_PROFILE_ID=
# 可选：Skyvern 代理地区；jp 会自动转 RESIDENTIAL_JP
SKYVERN_PROXY_LOCATION=
# 浏览器类型：默认 stealth-chromium；可选 msedge/chrome/stealth-chromium
SKYVERN_BROWSER_TYPE=
# 是否启用广告拦截
SKYVERN_AD_BLOCKER=
# 结束时是否保存 browser profile
SKYVERN_GENERATE_BROWSER_PROFILE=
# 调试时保留浏览器
SKYVERN_KEEP_BROWSER_OPEN=
# 起始 URL
SKYVERN_START_URL=

# ---- RoxyBrowser ----
# Roxy API 地址: 默认 http://127.0.0.1:50000；需在 Roxy 应用 API 配置中开启
ROXY_API_BASE=
# Roxy API Key: 保存在 .env（ROXY_API_TOKEN），不写回 config/*.py
ROXY_API_TOKEN=
# Roxy 环境ID: 指定要打开的 Roxy 浏览器环境/Profile ID；留空则尝试创建临时环境
ROXY_PROFILE_ID=
# Roxy 工作区ID: 创建一号一环境时必填，会作为 workspaceId 提交给 Roxy 创建 Profile 接口
ROXY_WORKSPACE_ID=
# Roxy 项目ID: 从 /browser/workspace 的 project_details.projectId 获取；创建 Profile 时会作为 projectId 提交
ROXY_PROJECT_ID=
# 获取团队接口: 默认 /browser/workspace；点击获取团队/项目时会先试此路径，再自动尝试常见兼容路径
ROXY_WORKSPACE_LIST_PATH=
# 打开接口路径: 默认 /browser/open；如 Roxy 版本不同可在此调整
ROXY_OPEN_PATH=
# 无头启动窗口: 打开 Roxy 环境时向 /browser/open 传 headless；False=显示窗口，True=无头启动
ROXY_OPEN_HEADLESS=
# 关闭接口路径: 默认 /browser/close
ROXY_CLOSE_PATH=
# 保留浏览器: 调试时可开启，任务结束后不自动关闭 Roxy 环境
ROXY_KEEP_BROWSER_OPEN=
# 一号一环境: 每个账号强制创建新 Roxy Profile，用完关闭并删除，禁止复用固定环境
ROXY_ONE_PROFILE_PER_ACCOUNT=
# 结束后删除环境: 一号一环境模式下，任务结束后删除本轮创建的 Roxy Profile
ROXY_DELETE_PROFILE_AFTER_RUN=
# 创建环境使用代理池: 创建 Roxy 环境时从配置页「代理池」随机取一个代理，写入 Roxy proxyInfo
ROXY_CREATE_USE_PROXY_POOL=
# 创建环境使用Roxy代理池: 从 RoxyBrowser 应用内配置的代理池(/proxy/list)随机取一条有效代理，
# 按 moduleId 引用(choose 模式)；开启后优先于上方项目代理池。Roxy 侧管理代理，本项目不接触代理账号密码
ROXY_CREATE_USE_ROXY_PROXY_POOL=
# 注册前避开重复IP: 检测真实出口 IP，命中历史账号/当前并发占用时换环境，最多 5 次
ROXY_AVOID_DUPLICATE_REGISTRATION_IP=
# 代理检测通道: 写入 Roxy proxyInfo.checkChannel；留空则不传，默认 IPRust.io
ROXY_PROXY_CHECK_CHANNEL=
# 删除接口路径: 默认 /browser/delete；如 Roxy 版本不同可调整
ROXY_DELETE_PATH=

# ---- CPA / Codex ----
# Codex授权驱动: protocol=原协议授权；roxy=用 RoxyBrowser；cloak=用 CloakBrowser；browser_use=用 Browser Use Cloud；skyvern=用 Skyvern；same_as_registration=跟随注册驱动
CODEX_OAUTH_DRIVER=

# ---- RoxyBrowser ----
# Codex回调超时: Roxy Codex OAuth 等待 localhost:1455 callback 的最长秒数
ROXY_CODEX_CALLBACK_TIMEOUT=

# ---- 功能开关 ----
# 注册时优先设置密码；True=先尝试密码注册，失败时由下方 OTP 兜底开关决定是否回验证码注册
REGISTER_SET_PASSWORD=
# 关闭 OTP 注册兜底；默认 False，保持密码注册失败后回退邮箱验证码
REGISTER_DISABLE_OTP_FALLBACK=
# 启用 2FA(TOTP): 注册完成后自动设置动态口令（会多收一封 OTP 邮件）
ENABLE_2FA=
# 2FA 补跑时顺带给 passwordless 账号设置一个随机密码（邮箱----密码----2FA密钥----AT 发货用）。
# 尽力而为，失败不影响 2FA 结果；设 False 可关闭。
BACKFILL_SET_PASSWORD=
# 启用 Flow 触发: 注册成功后自动调用内部 Flow 接口（不影响注册结果）
ENABLE_FLOW_TRIGGER=

# ---- 人工节奏 ----
# 启用随机停顿: 在注册、OTP、授权等步骤之间加入随机等待，更接近人工操作节奏
ENABLE_HUMANIZE_DELAY=
# 停顿倍率: 随机停顿整体倍率；1.0=默认，0.5=减半，2.0=加倍
HUMANIZE_DELAY_FACTOR=

# ---- 邮箱 / OTP ----
# 自动取邮箱+收码: True=从邮箱池自动领邮箱并自动收 OTP；False=手动模式：用 REGISTER_EMAIL，OTP 在任务页手填
USE_EMAIL_SERVICE=
# 手动注册邮箱: USE_EMAIL_SERVICE=False 时必填。例如你的 outlook.com 地址；OTP 去网页邮箱看，再回任务页提交
REGISTER_EMAIL=
# 显示名称: 留空则自动生成英文名
REGISTER_NAME=
# OTP 最长等待(秒): 等待验证码邮件的最长秒数，超时判失败
OTP_MAX_WAIT=
# OTP 轮询间隔(秒): 每隔多少秒查一次新邮件
OTP_POLL_INTERVAL=
# 邮箱来源: 可填单个或多个，逗号分隔并按顺序兜底：outlook,generic_api,imap_pass,mailcom,cloudflare_domain,cloudflare,gptmail,mailnest,cloudmail
EMAIL_SOURCE=
# Outlook取件模式: auto=远端优先，远端 402/DEPLOYMENT_DISABLED 自动切 Graph 直连；direct=只用 Microsoft Graph 直连；remote=只用远端服务
OUTLOOK_FETCH_MODE=
# mail.com / GMX / Caramail 共用邮箱池，均按 email----password 导入；mail.com 使用 maildotcom-sdk（Node.js 20+），GMX/Caramail 自动使用 IMAP SSL
# Node 可执行文件；留空从 PATH 自动查找
MAILCOM_NODE_BIN=
# SDK 登录态缓存目录；留空使用项目 run/mailcom_sessions
MAILCOM_SESSION_DIR=
# OAuth 登录及取信代理；留空优先复用 PROXY_PRE_PROXY，再用全局 PROXY；支持 HTTP/SOCKS
MAILCOM_PROXY=
# 单次 SDK 登录/拉信超时秒数，默认 60
MAILCOM_REQUEST_TIMEOUT=
# 每个文件夹读取的最近邮件数，默认 25（范围 1-100）
MAILCOM_MESSAGE_LIMIT=
# 仅 cloudflare_domain：Email Routing 转发域名，如 mydomain.com（与 EMAIL_SOURCE=cloudflare 无关）
EMAIL_DOMAIN=
# 仅 cloudflare_domain：接收 Email Routing 转发的 QQ 邮箱，如 123456@qq.com
QQ_EMAIL=
# 仅 cloudflare_domain：QQ 邮箱 IMAP 授权码（不写回 config/*.py）
QQ_IMAP_PASSWORD=
# GPTMail API Key: 选择 EMAIL_SOURCE=gptmail 时必填；服务地址固定为 https://mail.chatgpt.org.uk
GPTMAIL_API_KEY=

# Cloudflare Worker 临时邮箱（EMAIL_SOURCE 含 cloudflare 时；与 cloudflare_domain/QQ IMAP 不同）
# Worker API 根地址，例如 https://mail.example.com（不要末尾多余路径）
CLOUDFLARE_API_BASE=
# 匿名可空；admin 模式填 ADMIN_PASSWORD
CLOUDFLARE_API_KEY=
# none / bearer / x-api-key / x-admin-auth / query-key；admin 推荐 x-admin-auth
CLOUDFLARE_AUTH_MODE=
# Worker 全局密码，注入请求头 x-custom-auth；无则留空
CLOUDFLARE_CUSTOM_AUTH=
# 创建邮箱路径: 默认 /api/new_address；admin 创建常用 /admin/new_address
CLOUDFLARE_PATH_ACCOUNTS=
# 邮件列表路径: 默认 /api/mails
CLOUDFLARE_PATH_MESSAGES=
# 域名列表路径: 默认 /api/domains
CLOUDFLARE_PATH_DOMAINS=
# Token 路径: 默认 /api/token（fallback 预留）
CLOUDFLARE_PATH_TOKEN=
# 默认收信域名，逗号分隔，例如 mail.example.com；可留空由 Worker 决定
CLOUDFLARE_DEFAULT_DOMAINS=
# HTTP 请求超时秒数，默认 20
CLOUDFLARE_REQUEST_TIMEOUT=
# admin 创建时随机 local-part 长度，默认 10
CLOUDFLARE_NAME_LENGTH=
# MailNest-迈巢 Outlook 临时邮箱的 api key 获取页面 https://mailnest.top/account
MAIL_NEST_API_KEY=
# MailNest 项目代码：默认 chatgpt001
MAIL_NEST_PROJECT_CODE=
# CloudMail/Cloud Mail API 地址，例如 https://mail.example.com
CLOUDMAIL_API_BASE=
# CloudMail 管理员邮箱；填写账号密码后保存配置会自动获取 CLOUDMAIL_AUTH_TOKEN
CLOUDMAIL_ADMIN_EMAIL=
# CloudMail 登录密码
CLOUDMAIL_PASSWORD=
# CloudMail 生成 Token 接口路径，默认 /api/public/genToken
CLOUDMAIL_TOKEN_PATH=
# CloudMail/Cloud Mail API Authorization Token
CLOUDMAIL_AUTH_TOKEN=
# CloudMail 邮箱域名列表，每行一个
CLOUDMAIL_DOMAINS=
# 生成随机邮箱后是否自动调用 /api/public/addUser
CLOUDMAIL_AUTO_ADD_USER=
# 随机邮箱 local-part 长度
CLOUDMAIL_RANDOM_LOCAL_LENGTH=

# ---- 浏览器画像 ----
# 地区画像: 应与代理出口地区一致；可选 jp/cn/us/sg。当前本地代理实测为日本东京，推荐 jp
BROWSER_LOCALE_PROFILE=
# 按出口IP自动画像: 开启后每个 BrowserSession 会用当前代理出口 IP 自动选择语言/时区；失败时回退到地区画像
AUTO_BROWSER_LOCALE_FROM_IP=
# IP定位超时(秒): 出口 IP 地理信息接口的单次请求超时；接口失败会自动回退，不影响注册
IP_GEO_TIMEOUT=
# curl_cffi TLS 指纹: 留空用默认 chrome146；本代理池实测 chrome146 部分 IP 被 OpenAI 拒 403，可设 chrome124
IMPERSONATE=

# ---- 代理池 ----
# 代理池(每行一个): 每行一个代理 URL，留空行会被忽略；为空则不使用代理
PROXY_POOL=
# 代理前置通道(Pre-Proxy): 所有代理连接先经此前置代理再连目标代理，绕过代理商的源IP白名单。
# 例: socks5h://127.0.0.1:7892（本机 Clash）。仅支持 socks5/socks5h；留空=直连
PROXY_PRE_PROXY=
# 打开窗口前代理测活: Cloak 启动前验证代理链，失败立即换池内下一条
PROXY_HEALTH_CHECK=
# 单条代理测活超时(秒)
PROXY_HEALTH_TIMEOUT=
# 套餐查询网络模式: auto=优先系统代理、再用专用代理/代理池；proxy=强制代理；direct=强制直连
PLAN_CHECK_PROXY_MODE=
# 套餐查询专用代理: 留空时 auto/proxy 从代理池选择；可能包含认证信息
PLAN_CHECK_PROXY=
# 套餐查询单次超时(秒)，默认 8
PLAN_CHECK_TIMEOUT=
# 套餐查询最多尝试次数，默认 2
PLAN_CHECK_MAX_ATTEMPTS=
# 套餐查询切换路径前等待(秒)，默认 0.5
PLAN_CHECK_RETRY_DELAY=
# 新注册账号首次未发现 Plus 试用时的复查延迟；0=不复查（默认）
PLAN_CHECK_REGISTRATION_RECHECK_DELAY=
# 注册后 OAICS 检测失败时切换代理的总尝试次数，默认 4
OAICS_MAX_ATTEMPTS=

# ---- PayPal协议提链 ----
# 提链后端：local=直接调用 OAI-PayPal-Extractor；remote=兼容旧 CDK 服务
EXTRACT_LINK_BACKEND=local
# 套餐检测确认 free + Plus 试用资格后是否自动提链
EXTRACT_LINK_AUTO=False
# 外部 OAI-PayPal-Extractor 项目目录；留空时使用代码默认路径
EXTRACT_LINK_PROJECT_PATH=
# 外部项目 Python；留空时自动使用项目 .venv
EXTRACT_LINK_PYTHON=
# 全局提链代理（可能包含认证信息）；为空时使用账号注册代理
EXTRACT_LINK_PROXY=
# PayPal 账单国家/支付方式
EXTRACT_LINK_COUNTRY=GB
EXTRACT_LINK_PAYMENT_METHOD=paypal
# 是否执行外部项目的 checkout/update 流程
EXTRACT_LINK_APPLY_CHECKOUT_UPDATE=True
# 成功链接倒计时分钟数，默认 60
EXTRACT_LINK_EXPIRY_MINUTES=60
# 本地/远程提链后台并发数
EXTRACT_LINK_WORKERS=3
EXTRACT_LINK_QUEUE_LIMIT=500
# 旧 remote 服务地址与 CDK（local 模式留空）
EXTRACT_LINK_API_BASE=
EXTRACT_LINK_CDK=
EXTRACT_LINK_TYPE=pix
EXTRACT_LINK_REQUEST_TIMEOUT=30
EXTRACT_LINK_EVENT_TIMEOUT=180

# ---- 1K50 CDK 网页后端（EXTRACT_LINK_BACKEND=cdk_web；与 local/remote 路线互斥）----
# 开启后由 Paypal协议页管理多条 CDK；完整 CDK 只存 data/cdk_pool.json
CDK_WEB_ENABLED=False
CDK_WEB_BASE_URL=https://www.1k50.xyz/pp-cdk-vak
# 工作台密码可选；当前部署无需密码时保持空白
CDK_WEB_WORKBENCH_PASSWORD=
CDK_WEB_REQUEST_TIMEOUT=30
CDK_WEB_TASK_TIMEOUT=900
CDK_WEB_POLL_INTERVAL=2
CDK_WEB_PAYMENT_TIMEOUT=900
CDK_WEB_PAYMENT_POLL_INTERVAL=2
CDK_WEB_COUNTRY=GB
CDK_WEB_PROTOCOL_COUNTRY=GB
CDK_WEB_BUYER_MODE=identity_elevation
CDK_WEB_AUTO_PAYMENT=True
CDK_WEB_AGREEMENT_ONLY=True
# 为空时使用账号注册代理
CDK_WEB_PROXY=
CDK_WEB_SMS_MODE=server-auto
CDK_WEB_SMS_PROVIDER=
CDK_WEB_SMS_API_KEY=
CDK_WEB_SMS_COUNTRY=GB
# 额外重试轮数；可轮换下一条可用 CDK
CDK_WEB_MAX_RETRIES=2
CDK_WEB_WORKERS=2
CDK_WEB_QUEUE_LIMIT=500

# ---- PayPal BA 协议支付 ----
# 提链成功后是否自动进入协议支付；默认关闭，配置 SMSBower 后再开启
PAYPAL_PAYMENT_AUTO=False
# 完整 paypal-agreement-protocol 项目目录与可选 Python 路径
PAYPAL_PAYMENT_PROJECT_PATH=
PAYPAL_PAYMENT_PYTHON=
# 本地协议服务；未运行时可由 WebUI 自动启动 web.py/.integration-web.py
PAYPAL_PAYMENT_SERVICE_BASE=http://127.0.0.1:18097
PAYPAL_PAYMENT_AUTOSTART_SERVICE=True
PAYPAL_PAYMENT_SERVICE_START_TIMEOUT=20
PAYPAL_PAYMENT_PROTOCOL_TIMEOUT=600
PAYPAL_PAYMENT_POLL_INTERVAL=2
# 统一账单国家与支付代理；代理为空时使用每个账号的注册代理
PAYPAL_PAYMENT_COUNTRY=GB
PAYPAL_PAYMENT_BUYER_MODE=identity_elevation
PAYPAL_PAYMENT_AGREEMENT_ONLY=False
PAYPAL_PAYMENT_PROXY=
# SMSBower 接码服务固定为 PayPal；国家为平台 country ID，渠道号可留空
PAYPAL_PAYMENT_SMS_API_BASE=https://smsbower.page/stubs/handler_api.php
PAYPAL_PAYMENT_SMS_API_KEY=
PAYPAL_PAYMENT_SMS_SERVICE=paypal
PAYPAL_PAYMENT_SMS_COUNTRY=16
PAYPAL_PAYMENT_SMS_PROVIDER_IDS=
PAYPAL_PAYMENT_SMS_TIMEOUT=120
PAYPAL_PAYMENT_SMS_POLL_INTERVAL=3
# 取码失败、验证码失败和协议支付失败都消耗一次并更换号码
PAYPAL_PAYMENT_MAX_RETRIES=2
PAYPAL_PAYMENT_WORKERS=2
PAYPAL_PAYMENT_QUEUE_LIMIT=500

# ---- CPA / Codex ----
# 授权地址来源: cpa=由 CPA 管理接口生成授权地址；local=旧版本地生成 PKCE 授权地址
CODEX_AUTH_URL_SOURCE=
# CPA 管理地址: 例如 http://localhost:8317/admin/oauth；程序会取 origin 调用 /v0/management/*
CPA_MANAGEMENT_URL=
# 管理密钥: 保存在 .env（CPA_MANAGEMENT_KEY），不写回 config/*.py
CPA_MANAGEMENT_KEY=
# CPA 超时(秒): 请求 CPA 管理接口的超时时间
CPA_REQUEST_TIMEOUT=
# 保存CPA回执: CPA 未返回完整授权文件时，本地仍保存一份回调提交记录
CPA_SAVE_CALLBACK_RECEIPT=

# ---- 接码平台 ----
# 接码通道: grizzly / smsbower / l / h；grizzly=GrizzlySMS、smsbower=SMSBower（均 sms-activate 协议），h 使用 H_API.md 定义的本地取号服务
SMS_PROVIDER=
# 国家代码: 传给接码平台的 country；GrizzlySMS/SMSBower 常用：葡萄牙=117、美国=187；L/H 通道按本地后台配置填写
SMS_COUNTRY=
# 服务/项目代码: GrizzlySMS/L 作为 service；H 通道可复用为 projectId；SMSBower 用下方 SMSBOWER_SERVICE
SMS_SERVICE=
# 换号重试次数: 一个号收不到短信/被OpenAI拒时换下一个号，最多重试几次
SMS_MAX_RETRIES=
# 单号等短信(秒): 单个号等待短信到达的最长秒数，超时则换号
SMS_CODE_WAIT=
# 查短信间隔(秒): 轮询接码平台查短信的间隔
SMS_POLL_INTERVAL=
# GrizzlySMS API密钥: 保存在 .env（SMS_API_KEY），不写回 config/*.py
SMS_API_KEY=
# ---- SMSBower（SMS_PROVIDER=smsbower）----
# SMSBower API 地址: 默认 https://smsbower.page/stubs/handler_api.php
SMSBOWER_API_BASE=
# SMSBower API密钥: 在 smsbower.app 后台获取；保存在 .env（SMSBOWER_API_KEY），不写回 config/*.py
SMSBOWER_API_KEY=
# SMSBower 服务码: OpenAI(ChatGPT) 固定为 dr
SMSBOWER_SERVICE=
# 渠道号(providerIds): 指定渠道，逗号分隔如 3170,4120；留空则平台自动选渠道。可用 getPricesV3 按国家+服务查渠道号
SMS_PROVIDER_IDS=
# H API 地址: H 取号服务基础地址，例如 http://localhost:8788
H_API_BASE=
# H 授权码: 保存在 .env（H_ADMIN_AUTH_CODE），不写回 config/*.py
H_ADMIN_AUTH_CODE=
# H 号码前缀: H 返回号码不含国家码时填写，例如美国 10 位本地号填 1；留空则不补
H_PHONE_PREFIX=
# H 取号方式: reusable=优先复用号码；new=每次取新号（调用 /api/admin/h/take-phone）
H_PHONE_ACQUIRE_MODE=
# L API 地址: L 取号服务基础地址，例如 http://localhost:8788
L_API_BASE=
# L 授权码: 保存在 .env（L_ADMIN_AUTH_CODE），不写回 config/*.py
L_ADMIN_AUTH_CODE=
# L 号码前缀: L 返回号码不含国家码时填写，例如美国 10 位本地号填 1；留空则不补
L_PHONE_PREFIX=
